---
description: Windows native builds use CMake/Ninja under MSYS2 UCRT64
alwaysApply: true
---

# Windows Build Environment (MSYS2 UCRT64)

Use the checked-in CMake presets. Build presets already select eight parallel
Ninja jobs.

- Windows: `ucrt64-debug` or `ucrt64-release`
- Linux/macOS GCC: `gcc-debug` or `gcc-release`
- Linux/macOS Clang: `clang-debug` or `clang-release`

On Windows PowerShell, run native tools and bash scripts through the repository
wrapper:

```powershell
.\scripts\Invoke-Ucrt64.ps1 "cmake --preset ucrt64-debug"
.\scripts\Invoke-Ucrt64.ps1 "cmake --build --preset ucrt64-debug"
.\scripts\Invoke-Ucrt64.ps1 "ctest --preset ucrt64-debug"
.\scripts\Invoke-Ucrt64.ps1 "./scripts/compile-ui-tests.sh"
.\scripts\Invoke-Ucrt64.ps1 "./scripts/update-translations.sh"
```

`scripts/Invoke-Ucrt64.ps1` auto-detects MSYS2. Override its search with
`MSYS2_ROOT` when necessary. For gdb and clangd, use the UCRT64 `bin` directory
under that root.
